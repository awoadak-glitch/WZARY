/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/WZARY',
  assetPrefix: '/WZARY/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
